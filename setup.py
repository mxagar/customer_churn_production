from setuptools import setup

setup(name="customer_churn", # name of the package
	  version="0.1",
	  description="Customer churn computation library", 
	  packages=['customer_churn'], # folder names
	  zip_safe=False)